# vim: ft=yaml

extends:
  # dependencies: eslint-plugin-react
  - presets/misc/react.yml

  # env
  - presets/env/browser.yml
  - presets/env/node.yml

  # use recommended rules
  - eslint:recommended

  # custom rules
  - presets/rules/base.custom.yml

  # use react custom rules
  - presets/rules/react.custom.yml
